# تقرير تدقيق الجودة — 2026-09-11

> النطاق: فحص دقيق وشامل ومحايد لاستخراج الأخطاء والنقائص والعيوب المتبقية قبل النشر.
> الإصدار الناتج من هذا التدقيق: **1.9.3** (المحرك `1.6.2`، المخطط `3.4` دون تغيير).
> المنهج: فحص ثابت للكود والمستندات والنصوص المثبّتة، ثم إعادة تشغيل كامل منظومة التحقق (اختبارات، lint، typecheck، بناء، فحص دخان حي).

---

## 1) عيوب وُجدت وعولجت في هذا التدقيق

| # | الموضع | العيب | الإصلاح |
|---|--------|-------|---------|
| 1 | `scripts/smoke_deployment.py` | فحص الدخان كان مثبّتاً على `1.3.2` / schema `2.1` / engine `1.1.1`، أي كان سيفشل دائماً على أي إصدار حالي حتى لو كان النشر سليماً (فشل كاذب). | أُعيدت كتابته ليقرأ الإصدارات المتوقعة من المصدر (`__version__`, `ENGINE_VERSION`, `SCHEMA_VERSION`) مع إمكانية التجاوز بمتغيرات بيئة `PECHE_TN_EXPECTED_*`، وfallback لاتساق `root==health` خارج المستودع. |
| 2 | `apps/api/src/spotdata/domain/engine.py` (`_fouling_evidence`) | فرع `dense_fouling` ميّت وغير قابل للوصول: بلاغ كثافة واحد كان يُلتقط أصلاً بقاعدة البلاغ الواحد؛ الفرع الميت يخالف التصميم المعتمد «عدة بلاغات متسقة ⇒ قاهر». | حُذف الفرع الميت. |
| 3 | `components/DecisionApp.tsx:114` | عند الانتقال من انقطاع إلى اتصال كان يضبط نسخة الواجهة على `"1.7.0"` مثبّتة (ادعاء كاذب لإصدار). | صار الانتقال يُطلق فحص صحة جديداً (`checkApi`) ويضبط الحالة `checking` بدل تلفيق إصدار. |
| 4 | `app/calendar/page.tsx:124` | تذييل يعرض `الإصدار 1.8.0` ثابتاً. | أصبح `1.9.3`. |
| 5 | `compose.yaml` | لم يُضبط `PORT`؛ الحاوية (Dockerfile يستخدم `${PORT:-10000}`) كانت ستستمع على 10000 بينما Compose يربط ويختبر صحة المنفذ 8000. | أُضيف `PORT: "8000"`. |
| 6 | `README.md` (سطر عقد API) | مثال العقد يقول `schema_version: 2.1` و`engine_version: 1.1.1` بينما الفعلي `3.4` و`1.6.2`. | حُدّث إلى `3.4` / `1.6.2`. |
| 7 | `RENDER.md`, `VERCEL.md`, `TERMUX-GITHUB.md`, `apps/api/README.md` | ترويسات تثبّت الإصدار `1.3.2`. | أصبحت بلا إصدار مكتوب يدوياً + إشارة إلى `CHANGELOG.md` كمصدر وحيد للإصدار الحالي. |
| 8 | `docs/architecture.md`, `docs/local-api-policy.md` | عناوين تثبّت `1.3.2`. | أصبحت بلا إصدار. |
| 9 | `package-lock.json` | نسخة الجذر ونسخة `packages[""]` بقيتا على `1.3.2` (لم تُرفعا مع أي رفع سابق) — عدم اتساق lockfile مع `package.json`. | جرى تزامنهما إلى `1.9.3`. |

**النتيجة:** فحص `grep` عابر لم يعد يجد أي نسخة قديمة (`1.7.0`/`1.8.0`/`الإصدار 1.9.2`/`schema 2.1`/`engine 1.1.1`) في الكود والمستندات، بعد استثناء المراجع المشروعة أدناه.

---

## 2) ما فُحص وتبيّن أنه **ليس عيباً** (دقة التدقيق، لا تطمين)

- `e2e/responsive.spec.ts:194-195` — fixture الاختبار يحمل مسبقاً `schema_version: "3.4"` و`engine_version: "1.6.2"` (حالي، لا يحتاج تغييراً).
- `CHANGELOG.md` — إدخالات `1.3.2` سجل تاريخي مشروع، يُبقى كما هو.
- `docs/ENGINE-V2-SPEC.md:205` — «متسق مع قرار 1.3.2 القائم» إشارة تاريخية لسياسة قرار، صحيحة.
- `docs/SOURCES-AUDIT.md:3` — ترويسة مؤرخة `2026-09-10 — 1.8.0` سجل زمني لتاريخ إنجاز التدقيق، لا تثبيت إصدار تشغيلي.
- `TERMUX-GITHUB.md` — مراجع `1.3.2` المتبقية مثال إصدار موثق، وأُضيف تنبيه علوي للاستبدال بالإصدار الحالي.
- `requirements.txt` — التثبيتات (`fastapi==0.141.1`, `httpx==0.28.1`, `pydantic==2.13.5`, `tzdata==2026.3`) متسقة مع الاختبارات، بلا تعارض.
- لا توجد علامات `TODO`/`FIXME` في الكود.
- `lib/types.ts` — أنماط الواجهة تطابق أنماط/قيم الـ backend (`DecisionLevel`, `DecisionReasonCode`, `ForceMajeureKind`, `ShoreType`, …) بلا انحراف.

---

## 3) قيود معروفة ومقصودة (موثقة، ليست عيوباً خفية)

- **جلب نشرة INM الآلي غير مطبّق عمداً**: لا واجهة آلية مستقرة لدى INM (الجداول تُعرض من جهة العميل). التطبيق لا يزعم زوراً «لا تحذير رسمي»؛ البوابة اليدوية + الروابط الرسمية هي المسار المعتمد (D14).
- **طبقة Render المجانية تنام بعد ~15 دقيقة خمول** (توصية النشر السابقة).
- **Overpass orientation** قد يهبط `ReadTimeout` أحياناً (رُصدت تحذيرات أثناء الفحص) → يعود `Unknown` ولا يُلفَّق؛ فحص الدخان يتحقق من «لا تلفيق».
- **معايرة عتبة سحب التيار** الحساسة ومؤجلة عمداً مع معايرة سجل الصيد.
- **نثر Gemini اختياري** (يتطلب مفتاحاً).

---

## 4) نتائج التحقق النهائية (على 1.9.3)

| الفحص | النتيجة |
|-------|---------|
| `pytest` | **202 passed**, 3 warnings |
| `ruff check` | نظيف |
| `tsc --noEmit` | نظيف |
| `eslint` (app/components/lib) | نظيف |
| `npm run build:render` | نجح (تصدير static) |
| `scripts/smoke_deployment.py http://localhost:8001` | **ALL PASS** — `serving Peche TN 1.9.3 frontend and API contracts` |
| عقد القرار (حي) | `go`، 63 عاملاً، تاريخ 48–72 ساعة، Sentinel‑2 غير متوفر |
| عقد التوجيه (حي) | resolved دون تلفيق |
| `/api/health` | `1.9.3` / engine `1.6.2` / schema `3.4` |
| تذييلات الواجهة (حي) | `/` و`/calendar` تعرضان `الإصدار 1.9.3` |

---

## 5) خلاصة محايدة

- وُجد **9 عيوب حقيقية** (أغلبها تثبيت إصدارات قديمة في الفحص والمستندات والقفل والواجهة، وفرع ميت في المحرك، وتهيئة ناقصة في Compose). عولجت كلها وأُثبت العلاج بالفحص والبناء والفحص الحي.
- لا يوجد حالياً أي عيب وظيفي معروف يكسر القرار الثنائي `go/no_go` أو السلامة أو قابلية التنفيذ.
- القيود المتبقية كلها موثقة ومقصودة (انظر القسم 3).
